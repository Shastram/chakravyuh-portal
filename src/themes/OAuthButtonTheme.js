import { createTheme } from '@mui/material/styles';
let OAuthButtonTheme = createTheme({
    palette: {
        primary: {
            main: '#9fa2a6',
        },
    },
});

export default OAuthButtonTheme;