import { createTheme } from '@mui/material/styles';
let ButtonTheme = createTheme({
    palette: {
        primary: {
            main: '#7f70f1',
        },
        text: {
            primary: "#fff"
        }
    },
});

export default ButtonTheme;